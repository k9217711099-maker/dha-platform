# CLAUDE.md

Гид по проекту для Claude Code. Составлен на основе ТЗ `Промт.docx`.

## Что это за проект

Единая цифровая платформа для гостей сети **D Hotels & Apartments (D H&A)**:
мобильные приложения iOS/Android, веб-версия личного кабинета на сайте,
собственный модуль бронирования (booking engine), программа лояльности,
онлайн-регистрация гостя, цифровой ключ через TTLock.

Бизнес-цель: рост доли **прямых** бронирований, повторных проживаний и среднего
чека, снижение нагрузки на администраторов.

Статус: проектирование/старт разработки. Кода пока нет — документ ТЗ и набор
дизайн-скиллов в `skills/`.

## Ключевой архитектурный принцип (не нарушать)

```
Гость → мобильное приложение / сайт → backend D H&A ─┬─ собственные модули (ядро)
                                                     │    ├─ PMS / Booking / Rate Engine + Channel Manager (DHP)
                                                     │    ├─ Программа лояльности
                                                     │    ├─ Коммуникации + AI-агенты
                                                     │    ├─ Задачи и Уборка (Operations)
                                                     │    ├─ Мессенджер сотрудников
                                                     │    └─ База знаний + Диск
                                                     └─ внешние системы
                                                          ├─ Bnovo PMS (опционально, легаси-импорт)
                                                          ├─ OTA-каналы (через Channel Manager)
                                                          ├─ TTLock
                                                          ├─ платёжный провайдер (YooKassa)
                                                          └─ SMS / email / push / мессенджеры
```

**Клиенты (мобайл/веб) НИКОГДА не обращаются напрямую к Bnovo, TTLock, OTA или
платёжному провайдеру.** Все внешние интеграции идут только через backend D H&A.
Причины: безопасность API-ключей, контроль бизнес-логики, единая база гостей,
независимая программа лояльности, защита ПДн, возможность сменить PMS в будущем.

### Источники истины
- **Объекты, номерной фонд, доступность, тарифы, брони** → **собственный PMS D H&A**
  (Путь B / DHP, см. ниже). Bnovo — опциональный импорт/легаси, из ядра выведен.
- **Программа лояльности** → backend D H&A (собственная система, не зависит от Bnovo).
- **Коммуникации, CRM-переписка, задачи, уборка, база знаний** → **собственные модули
  D H&A** (не Bitrix24). Bitrix24 из ядра выведен; интеграция с ним — опциональна и не
  является источником истины. Рабочее место сотрудника — админка D H&A. Профильные ТЗ:
  `AI-COMMUNICATIONS-TZ.md` (коммуникации + AI-агенты), `TASKS-HOUSEKEEPING-TZ.md`
  (задачи/уборка, модуль `ops/*`), `STAFF-CALLS-TZ.md`, `KB-DRIVE-TZ.md` (база знаний + диск),
  внутренний мессенджер сотрудников. Заселение — `CHECK-IN-TZ.md`.

### Путь B: собственный PMS + Channel Manager (DHP) — решение владельца 2026-07-02
Строим свой PMS/Booking/Rate Engine + Channel Manager вместо надстройки над Bnovo
(снимает блокер «Bnovo Open API не создаёт брони», убирает вендор-lock). Спека —
папка `DHP-claude-code-start/` (трактуем как референс, НЕ как скаффолдер: пустой репозиторий
не создаём — встраиваем в это монорепо). Согласованные отклонения от DHP-спеки: **развиваем
существующие модели** (Property/RoomType/Booking + новый Room-юнит, `tenantId` мультиарендно,
`bnovoId` опционален), **мультиарендность (SaaS)** с самого старта, **сохраняем наш стек**
(NestJS+Prisma+встроенный Postgres, админка Next.js, очередь синка на БД+`@nestjs/schedule`,
Redis/BullMQ и Docker — позже/опционально). Ключевые правила: **овербукинг невозможен**,
`Idempotency-Key` на создание брони, проверка доступности в PG-транзакции, дата выезда ночь не
занимает, синк каналов не блокирует создание брони. PMS-модуль: `apps/api/src/pms/*` (права
`pms_*`, роли DHP §5). План/прогресс — `.claude/plans/serene-frolicking-tiger.md`.

## Рекомендуемый стек

- **Мобайл:** React Native или Flutter.
- **Backend:** Node.js / NestJS либо Python / Django / FastAPI.
- **БД:** PostgreSQL. **Кэш:** Redis. **Очереди:** RabbitMQ / BullMQ / Celery.
- **Файлы:** S3-совместимое хранилище (сканы документов — с повышенной защитой).
- **Веб:** личный кабинет + booking engine, адаптивная вёрстка (мобайл + десктоп).

Стек ещё не зафиксирован окончательно — это рекомендации ТЗ. Уточнить выбор у
владельца перед генерацией бойлерплейта.

## Доменные правила, которые легко нарушить

### Программа лояльности (валюта — «баллы D»)
- 1 балл = 1 ₽ при списании.
- Баллы начисляются **только за оплаченные и завершённые прямые бронирования**
  (сайт, приложение, звонок, мессенджер, прямое бронирование через менеджера).
  За OTA-бронирования баллы можно не начислять.
- Не начисляются за отменённые бронирования; база начисления — стоимость
  проживания без налогов/депозитов/штрафов/возвратов/услуг партнёров.
- Уровни: **Member 3%** (регистрация) → **Silver 5%** (50 000 ₽ или 5 ночей / 12 мес,
  списание до 15%) → **Gold 7%** (150 000 ₽ или 15 ночей / 12 мес) →
  **Platinum 10%** (300 000 ₽ или 30 ночей / 12 мес). Пороги считаются по прямым
  бронированиям за скользящие 12 месяцев.
- Срок действия баллов: 24 месяца, продлевается ещё на 24 при новом прямом бронировании.
- Мин. списание 500 баллов. Баллами нельзя оплачивать депозит и штрафы.
- Статусы баллов: ожидают начисления / доступны / списаны / заморожены / сгорели / отменены.
  Баллы становятся «доступны» только после выезда и подтверждения оплаты.
- Часть привилегий (ранний заезд/поздний выезд/апгрейд) доступна **только в отелях
  сети, не в апартаментах/квартирах** — учитывать тип объекта.

### Цифровой ключ TTLock
- Ключ создаётся только если: бронь активна и не отменена, наступило разрешённое
  время выдачи, пройдена онлайн-регистрация, выполнены оплата/депозит (если требуется).
- Окно действия по умолчанию: за 30 мин до заезда — через 30 мин после выезда
  (настраивается в админ-панели). **После проживания пароль/доступ удаляется.**
- Типы доступа: PIN, Bluetooth eKey, через gateway, резервный код администратора,
  доступ для персонала. Логировать весь жизненный цикл ключа.

### Бронирования
- Раздел «Мои бронирования» делится строго на 4: текущие, предстоящие, прошлые,
  отменённые — у каждого свой набор полей и действий (см. ТЗ §7).
- При создании брони backend обязан: проверить актуальность цены/доступности в
  Bnovo → создать бронь в Bnovo → начислить/зарезервировать баллы → отправить
  подтверждение → показать в ЛК.
- Синхронизация с Bnovo: API + webhooks (если есть) + fallback-поллинг каждые
  5–15 мин, с журналом ошибок и ретраями.

## Брендбук / дизайн

- **Гостевой бренд «Тихий люкс» v1.2 (2026-07-17, палитра «Эспрессо»):** слоновая кость
  `#F7F4EE`, эспрессо `#3E362E` (тёплый тёмный вместо чёрного; «тушь» #1C1A17 и умбра
  #524A40 выведены), дым `#6B6156`, тёплый песок `#EFEAE0`, бронза `#A5794A`; шрифты
  Cormorant Garamond (заголовки) + Jost (текст), прямые углы, hairline-линейки. Полный
  брендбук — `brand/BRANDBOOK.md`, AI-инструкция — `brand/BRAND-AI.md`, эталон —
  `design-variants/01a-espresso.html`.
  Применяется ко всему гостевому (сайт `apps/web`, мобайл, письма, печать).
- **Админка — отдельная служебная тема «Спокойный индиго» (Manrope)** в
  `packages/ui/tailwind-preset` / `tokens.css`; гостевая тема переопределяется локально
  в `apps/web/tailwind.config.ts` + `apps/web/src/app/globals.css`. Пресет не трогать.
- Исходный беж `#e7e3dd` (PANTONE P 169-1 C) и Onest — легаси из ТЗ: близкий тон остался
  в палитре как «тёплый песок» `#EFEAE0`, Onest пока живёт в мобильном приложении.
- Логотип — по ссылкам из ТЗ (уточнить у владельца); пока используется текстовая
  композиция «D H&A» (см. брендбук §2).
- Тон: премиальный, сдержанный, гостиничный люкс.

При любой работе над UI использовать скиллы из `skills/`:
- `skills/ui-ux-pro-max/` — палитры, типографика, UX-гайдлайны, стеки.
- `skills/frontend-design/` — production-grade, небанальный фронтенд.

## Объём работ (MVP vs v2)

**MVP (v1):** регистрация/авторизация, профиль, поиск + фильтры, собственный
модуль бронирования, 4 раздела бронирований, программа лояльности, онлайн-
регистрация, автоматизированное заселение (воронка check-in, см. `CHECK-IN-TZ.md`),
собственные модули коммуникаций/задач/уборки (вместо Bitrix24), базовый ключ
TTLock (PIN + eKey если возможно), push, веб-ЛК.

**v2:** расширенные апселлы, заявки на обслуживание, полноценный чат с ресепшен,
AI-консьерж, рекомендации района, отзывы, персональные предложения, расширенная
аналитика, сегментация, интеграции с платежами и фискализацией.

> Раздел «Заявки на обслуживание» и чат с ресепшен: перед планированием ТЗ требует
> отдельно обсудить с владельцем возможность AI-чат-бота (единое окно, разбор
> заявок, ответы на сторонние вопросы по загруженной базе знаний). Не реализовывать
> молча — сначала согласовать.

## Безопасность и ПДн

HTTPS, JWT/OAuth2 + refresh tokens, 2FA для администраторов, журнал действий
админов, разграничение прав. Паспортные данные и сканы документов — повышенный
уровень защиты: шифрование файлов, ограничение доступа, срок хранения, удаление по
регламенту, логирование просмотров.

## Открытые вопросы до старта (ТЗ §23)

Не делать допущений по этим пунктам — уточнять у владельца: доступные методы Bnovo
API в текущем тарифе (создание/изменение/отмена брони, конкретные номера vs
категории, статусы оплаты); парк замков TTLock и наличие gateway; нужен ли eKey
или достаточно PIN; платёжный провайдер и фискализация; где хранится текущая база постоянных гостей;
учёт OTA в лояльности; юр. согласия при онлайн-регистрации; нужно ли отдельное
приложение для сотрудников; старт со сторов или с web/PWA.

## Деплой (как правильно накатывать обновления) — читать перед выкладкой

Прод — **nomero.online** (web :3000, admin :3002, api :3001 за nginx). Единственный
корректный способ обновить прод — **`git push` в ветку `main`**. Дальше GitHub Actions
(`.github/workflows/deploy.yml`) сам заходит по SSH на сервер и запускает
`infra/deploy/deploy.sh main`. Ветка `develop` → staging-сервер, `main` → production.

**Что делает `deploy.sh` (полный цикл — почему пуш «просто работает»):**
1. `git fetch origin <branch>` + `git reset --hard origin/<branch>` — жёстко на пушённый коммит.
2. `pnpm install --frozen-lockfile`.
3. **`prisma generate` + `prisma db push --accept-data-loss`** — генерит клиент и синкает схему.
4. Прописывает `NEXT_PUBLIC_API_URL` (запекается в бандл на сборке) и `GUEST_PORTAL_BASE_URL`.
5. **`pnpm build` — сборка НАЧИСТО:** кэш turbo/Next чистится в `deploy.yml` ДО сборки
   + `TURBO_FORCE`/`rm .next` в `deploy.sh`. Реальная полная сборка ~2–3 мин.
6. `pm2 startOrReload` + workflow делает `pm2 restart dha-api dha-web dha-admin` (жёсткий рестарт всех трёх).

**Сборка = НАЧИСТО, каждый деплой (важно — НЕ инкрементально).** Историческое (2026-07-18):
пробовали «вариант 1» (инкрементальный кэш turbo, без чистки) — и он **заморозил прод**:
turbo на этом сервере отдавал ложный `>>> FULL TURBO` cache-hit и восстанавливал **залипший
старый бандл**, деплой шёл зелёным, а на проде оставался старый код (новые роуты API = 404,
хотя `git reset` доходил до нужного коммита и pm2 рестартился). Поэтому:
- **Кэш turbo/Next чистим перед каждой сборкой** (`rm -rf .turbo apps/*/.next apps/*/.turbo
  node_modules/.cache` в `deploy.yml`, `TURBO_FORCE=true` в `deploy.sh`). Инкрементальному
  кэшу тут **не доверяем**.
- Это **дёшево**: по логам сборки admin ~38с, web ~22с, api секунды → весь деплой **~2–3 мин**.
  Пугавшие «17–50 мин» были не сборкой, а **штормом** параллельных деплоев (см. ниже), не временем сборки.

**Грабли и защита:**
- 🔒 **Concurrency-guard в `deploy.yml`** (`group: deploy-<branch>`, `cancel-in-progress: false`) —
  **настоящий фикс** «застрявших деплоев». Одновременно идёт **один** деплой на ветку, очередь
  по одному. Без него несколько быстрых пушей (напр. из разных чатов) запускали параллельные
  деплои → дрались за один сервер по SSH → падения за 3–40 сек. При работе в нескольких чатах:
  **деплой по очереди**, каждому чату — своя зона файлов (дерево одно, не редактировать одни и те же).
- 🔁 **Лаг самообновления `deploy.sh`:** скрипт делает `git reset` внутри себя, поэтому bash
  исполняет ПРЕДЫДУЩУЮ версию `deploy.sh`; правки в нём применяются со СЛЕДУЮЩЕГО деплоя.
  Всё, что должно сработать сразу, кладём в `deploy.yml` (он берётся из пушимого коммита).
- 🔧 **`pm2 restart` включает `dha-api`** (не только web/admin) — иначе API мог остаться на
  старом процессе. Строка кончается `|| true`, так что молчаливый сбой рестарта не красит прогон — проверять живьём.
- ⏱️ `command_timeout: 30m` в `deploy.yml` — запас (реально ~2–3 мин).
- ❌ **Не запускать голый `pnpm build` на сервере вручную** — пропускает `prisma generate`
  (→ TS-ошибки `RoomCreateInput` / устаревший Prisma-клиент). Только `bash infra/deploy/deploy.sh main`.
- ✅ `NEXT_PUBLIC_*` попадает в фронт **на этапе сборки**, а не в рантайме pm2 → менять адрес API =
  пересобирать web/admin (turbo это сделает сам, т.к. адрес в ключе кэша).
- ✅ После пуша ждать ~1–3 мин; в браузере — жёсткий refresh (Cmd+Shift+R). Service worker
  гостевого сайта **не кэширует** контент (только делает сайт устанавливаемым как PWA).

Коммитить/пушить — **только когда владелец попросил** («давай задеплоим» и т.п.). Мы на `main`
работаем напрямую (staging=`develop`).

## Замечания по работе

- Язык проекта и общения с владельцем — русский.
- ТЗ — единственный источник требований; при расхождениях сверяться с `Промт.docx`.
- Перед крупными решениями по стеку, интеграциям и AI-функциям — согласовывать.
