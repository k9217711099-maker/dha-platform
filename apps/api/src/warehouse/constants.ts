/**
 * Лимит суммы списания, выше которого требуется согласование руководителем (§17.7).
 * Сумма оценивается по средней цене позиций. ₽.
 */
export const WRITE_OFF_APPROVAL_LIMIT = 5000;

/**
 * Лимит суммы расхождения инвентаризации, выше которого утверждение корректировок
 * требует право руководителя (wh_approve_writeoff). ₽ (§5.6).
 */
export const INVENTORY_DISCREPANCY_LIMIT = 5000;
